locals {
  global_console_feature_map = {
    /*
    # Placeholder for when console plugin is needed
    "devops-deploy-deploy-initial-regions" = {
      name = "devops-deployment-deploy-initial-regions"
      description = "Set the initial region for devops deployment console plugin deployment"
      default_value = local.initial_deployment_region_for_realm
    },
    */
  }
}
